{{#if this.session.isAuthenticated}}
  Redirecting...
{{else}}
  <div class="min-h-screen bg-gray-50 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
    <div class="bg-white shadow py-8 px-4 sm:px-10 sm:rounded-lg sm:mx-auto sm:w-full sm:max-w-md">
      <div class="">
        <img class="mx-auto h-32 w-auto" src="/assets/images/logo.jpg" alt="Rollvolet">
        <h2 class="mt-6 text-center text-4xl font-extrabold font-display tracking-wide text-gray-900">
          Prijsbeheer
        </h2>
      </div>

      <div class="mt-8">
        <MsalLogin />
      </div>
    </div>
  </div>
{{/if}}
