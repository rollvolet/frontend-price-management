{{#if (or @date this.by)}}
  {{#if @resource}}
    {{@resource}} aangemaakt
  {{else}}
    Aangemaakt
  {{/if}}
  {{#if @date}}
    op {{format-date @date showTime=true}}
  {{/if}}
  {{#if this.by}}
    door {{this.by}}
  {{/if}}
{{/if}}