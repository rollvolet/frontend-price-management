{{#if (or @date this.by)}}
  {{#if @resource}}
    {{@resource}} aangemaakt
  {{else}}
    Aangemaakt
  {{/if}}
  {{#if @date}}
    op {{dayjs-format date=@date format="DD-MM-YYYY"}}
  {{/if}}
  {{#if this.by}}
    door {{this.by}}
  {{/if}}
{{/if}}