import snapshot from 'frontend-price-management/utils/snapshot';
import { module, test } from 'qunit';

module('Unit | Utility | snapshot', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = snapshot();
    assert.ok(result);
  });
});
