import formatDecimal from 'frontend-price-management/utils/format-decimal';
import { module, test } from 'qunit';

module('Unit | Utility | format-decimal', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = formatDecimal();
    assert.ok(result);
  });
});
