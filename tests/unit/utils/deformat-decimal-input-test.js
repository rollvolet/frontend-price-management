import deformatDecimalInput from 'frontend-price-management/utils/deformat-decimal-input';
import { module, test } from 'qunit';

module('Unit | Utility | deformat-decimal-input', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = deformatDecimalInput();
    assert.ok(result);
  });
});
